const ROUTE_INIT = "";

const ROUTES = {
  HOME: {
    ROUTE: `${ROUTE_INIT}`,
    NAME: "home",
  },
  PRODUCT: {
    ROUTE: `${ROUTE_INIT}/:id/details`,
    NAME: "product",
  },
};

export { ROUTES };
