export const getProductsData = (state) => state.list.data;

export const getProductsLoading = (state) => state.list.loading;

export const getProductsError = (state) => state.list.error;

export const getProductData = (state) => state.item.data;

export const getProductLoading = (state) => state.item.loading;

export const getProductError = (state) => state.item.error;
